# Global page TSconfig.
# Loaded automatically by the core (see Feature #96614, available since TYPO3 12.0).
# Replaces the former ExtensionManagementUtility::addPageTSConfig() call, which
# has been deprecated in TYPO3 13 and removed in TYPO3 14.
@import 'EXT:dlf/Configuration/TsConfig/ContentElements.tsconfig'
